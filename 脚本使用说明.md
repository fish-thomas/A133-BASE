# HelperBoard A133 RGB Display - 脚本使用说明

## 📦 脚本文件

项目包含以下便捷脚本：

| 脚本名称 | 功能 |
|---------|------|
| `build.sh` | 编译程序 |
| `run.sh` | 运行程序 |
| `build_and_run.sh` | 编译并运行 |

---

## 🚀 快速开始

### 1️⃣ 首次使用：设置脚本权限

```bash
cd /path/to/rgb_display_demo
chmod +x build.sh
chmod +x run.sh
chmod +x build_and_run.sh
```

### 2️⃣ 编译并运行（推荐）

```bash
./build_and_run.sh
```

---

## 📝 各脚本使用说明

### 1. build.sh - 编译脚本

**功能：** 清理旧构建 → qmake → make → 复制到主目录

**使用方法：**
```bash
./build.sh
```

**输出：**
- 自动清理 `build` 目录
- 调用 qmake 和 make 编译
- 编译成功后，可执行文件复制到 `~/rgb_display_demo`
- 显示编译结果和下一步操作提示

---

### 2. run.sh - 运行脚本

**功能：** 清理环境变量 → 设置参数 → 运行程序

**使用方法：**
```bash
./run.sh
```

**功能特性：**
- 自动清理所有可能干扰的 Qt 环境变量
- 设置正确的 `QT_QPA_PLATFORM=linuxfb`
- 显示程序运行状态和退出码

---

### 3. build_and_run.sh - 编译并运行脚本

**功能：** 先编译，成功后自动运行

**使用方法：**
```bash
./build_and_run.sh
```

---

## 🔧 手动操作（如果脚本有问题）

### 手动编译：
```bash
cd rgb_display_demo
rm -rf build
mkdir build && cd build
qmake ../rgb_display_demo.pro
make
cp rgb_display_demo ~/
```

### 手动运行：
```bash
cd ~

# 清理环境变量
unset QTDIR
unset LD_LIBRARY_PATH
unset QT_QPA_PLATFORM_PLUGIN_PATH
unset QML2_IMPORT_PATH
unset QT_QPA_PLATFORM
unset QT_QPA_FONTDIR
unset QT_QPA_EGLFS_INTEGRATION
unset QT_QPA_FB_TSLIB
unset TSLIB_CONSOLEDEVICE
unset TSLIB_CONFFILE
unset TSLIB_CALIBFILE
unset TSLIB_FBDEVICE
unset TSLIB_PLUGINDIR
unset TSLIB_TSDEVICE
unset QT_QPA_GENERIC_PLUGINS

# 设置显示参数
export QT_QPA_PLATFORM=linuxfb
export QT_QPA_EGLFS_WIDTH=800
export QT_QPA_EGLFS_HEIGHT=480

# 运行
./rgb_display_demo
```

---

## 💡 常见问题

### Q: 脚本提示没有权限？
A: 运行 `chmod +x *.sh` 添加执行权限。

### Q: 编译失败？
A: 检查系统是否安装了 Qt5 开发包：
```bash
sudo apt-get install qtbase5-dev qtbase5-dev-tools
```

### Q: 运行时找不到平台插件？
A: 确认已用 `run.sh` 脚本启动，它会清理所有干扰的环境变量。

---

## 📋 项目目录结构

```
rgb_display_demo/
├── build.sh              ← 编译脚本
├── run.sh                ← 运行脚本
├── build_and_run.sh      ← 编译并运行脚本
├── 脚本使用说明.md       ← 本文档
├── README.md             ← 项目说明
├── main.cpp              ← 程序入口
├── mainwindow.h/cpp      ← 主窗口
├── wifidialog.h/cpp      ← WiFi设置对话框
├── terminalwidget.h/cpp  ← 终端部件
└── rgb_display_demo.pro  ← Qt项目文件
```
